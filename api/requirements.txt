fastapi==0.116.1
uvicorn[standard]==0.35.0
psycopg[binary]==3.2.3
python-multipart
pdfplumber==0.11.4
# Renderiza páginas de PDF a imagen (necesita el binario `pdftoppm` de
# poppler-utils, instalado en el Dockerfile) -- usado solo por
# services/fotos_pdf.py para recortar las fotos del alumnado.
pdf2image==1.17.0
requests==2.32.3
# NER en español para el anonimizador de Herramientas IA (services/anonimizador.py).
# El modelo se instala como wheel directo en vez de "spacy download" para no
# depender de una descarga en tiempo de build sin pinning de versión.
spacy==3.8.2
es_core_news_md @ https://github.com/explosion/spacy-models/releases/download/es_core_news_md-3.8.0/es_core_news_md-3.8.0-py3-none-any.whl
python-docx==1.1.2
python-pptx==1.0.2
# Red de seguridad para el JSON que pega el profesor tras copiar/pegar en una
# IA online (o el que devuelve Groq): arregla comillas sin escapar dentro de
# cadenas y otros fallos de formato habituales que json.loads(strict=False)
# no cubre -- ver services/prompts/situacion_aprendizaje.py e
# instrumento_evaluacion.py.
json_repair==0.63.4
